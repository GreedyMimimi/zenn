---
title: XRPLでNFTをミント・取引する：初心者向けハンズオンガイド
emoji: 🎨
type: tech
topics: [xrpl, nft, blockchain, javascript, marketplace]
published: true
---

## はじめに

XRP Ledger（XRPL）は **ガス代がなく、スピーディ** だからこそ、NFTの発行・取引に向いています。イーサリアムのようにスマートコントラクトを書く必要もありません。プロトコルレベルで **NFT機能が組み込まれている** のです。

この記事では、XRPL上で実際にNFTをミント（発行）し、売却・買入までを一連で体験します。難しいコードはなし。実装コードとコマンドで、すべてが動きます。

### この記事で学べること

- XRPL NFT の仕組み（XLS-20）
- `xrpl.js` を使った NFT ミント手順
- NFT の売却リスト作成と買い手側の処理
- Testnet で実際に動かす方法

---

## XRPL NFT の基礎：XLS-20 とは

XRPL の NFT 実装は **XLS-20** というプロトコル拡張で定義されています。特徴：

- **スマートコントラクト不要** — プロトコルがネイティブに NFT をサポート
- **ガス代ナシ** — トランザクション手数料は固定（~0.00001 XRP）
- **即座に確定** — ブロック確認時間が短い（数秒）
- **ロイヤリティ組み込み** — クリエイターへの自動ロイヤリティ支払い可能

### NFT トークン ID（TokenID）の仕組み

XRPL NFT は **TokenID** で一意に識別されます：

```
000800001234567890ABCDEF1234567890ABCDEF
```

構成：
- **Sequence** (16進数 8 桁)：このアカウントの何番目の NFT か
- **Scramble** (32 桁)：重複回避のためのハッシュ値

> つまり、同じアカウントが 2 回ミントすれば、Sequence は違う値になり、別の NFT になります。

---

## セットアップ：環境構築

### 必要なもの

1. Node.js 18 以上
2. `xrpl.js` パッケージ
3. Testnet アカウント（練習用）
4. XRP（Testnet なら無料配布）

### インストール

```bash
npm init -y
npm install xrpl dotenv
```

### .env ファイルを作成

```env
TESTNET_SEED=rN7n7otQDd6FczFgLdZqwcjwyvWtQaYeMi...  # あなたの Secret Seed
TESTNET_PUBLIC_ADDRESS=rN7n7otQDd6FczFgLdZqwcjwyvWtQaYeMi...  # あなたのアカウント
```

> **Secret Seed の取得方法：**
> https://testnet.xrpl.org にアクセス → 「Generate」で新規アカウント生成 → Seed をコピー

---

## ステップ 1：NFT をミント（発行）する

XRPL でのミント操作は **`NFTokenMint` トランザクション** を送信することです。

### コード例

```javascript
const xrpl = require("xrpl");
require("dotenv").config();

const client = new xrpl.Client("wss://s.altnet.rippletest.net:51233");

async function mintNFT() {
  await client.connect();

  const wallet = xrpl.Wallet.fromSeed(process.env.TESTNET_SEED);

  // NFT ミント用トランザクション
  const transactionJson = {
    TransactionType: "NFTokenMint",
    Account: wallet.address,
    URI: xrpl.convertStringToHex("https://example.com/nft-metadata.json"),
    Flags: 0x00000008,  // Burnable フラグ（後で焼却可能）
    TransferFee: 0,  // ロイヤリティ（0% = 取引時にクリエイターへ自動支払いなし）
  };

  // トランザクションに署名して送信
  const tx = await client.submitAndWait(transactionJson, { wallet });

  console.log("✅ NFT ミント成功！");
  console.log("Transaction Hash:", tx.result.hash);
  console.log("Ledger Index:", tx.result.ledger_index);

  // ミント後のアカウント情報を確認
  const accountInfo = await client.request({
    command: "account_info",
    account: wallet.address,
  });

  console.log("Account NFT Count:", accountInfo.result.account_data.account_nft_count || 0);

  await client.disconnect();
}

mintNFT().catch(console.error);
```

### 実行してみる

```bash
node mint.js
```

**出力例：**
```
✅ NFT ミント成功！
Transaction Hash: E4F8B8...
Ledger Index: 12345678
Account NFT Count: 1
```

---

## ステップ 2：ミント済み NFT を確認する

ミント後、**自分のアカウントに所有 NFT リストを問い合わせ** できます。

```javascript
async function listMyNFTs() {
  await client.connect();

  const wallet = xrpl.Wallet.fromSeed(process.env.TESTNET_SEED);

  const response = await client.request({
    command: "account_nfts",
    account: wallet.address,
  });

  console.log("📋 あなたが所有する NFT:");
  response.result.account_nfts.forEach((nft, index) => {
    console.log(`\nNFT ${index + 1}:`);
    console.log(`  TokenID: ${nft.token_id}`);
    console.log(`  Issuer: ${nft.issuer}`);
    console.log(`  Burned: ${nft.burned}`);
  });

  await client.disconnect();
}

listMyNFTs().catch(console.error);
```

---

## ステップ 3：NFT を売却リストに登録する

あなたの NFT を他の人に売却したい場合、**`NFTokenCreateOffer` トランザクション** を使って「売却オファー」を作成します。

```javascript
async function createSellOffer(tokenID, priceInXRP) {
  await client.connect();

  const wallet = xrpl.Wallet.fromSeed(process.env.TESTNET_SEED);

  // 売却オファー作成
  const transactionJson = {
    TransactionType: "NFTokenCreateOffer",
    Account: wallet.address,
    TokenID: tokenID,
    Amount: xrpl.xrpToDrops(priceInXRP),  // XRP を Drops（最小単位）に変換
    Flags: 0x00000001,  // Sell フラグ（これが売却オファー）
  };

  const tx = await client.submitAndWait(transactionJson, { wallet });

  console.log(`✅ 売却オファー作成成功！`);
  console.log(`TokenID: ${tokenID}`);
  console.log(`Price: ${priceInXRP} XRP`);
  console.log(`Offer Index: ${tx.result.offer_index}`);

  await client.disconnect();
}

// 実行例：TokenID を指定して 10 XRP で売却
createSellOffer("000800001234567890ABCDEF1234567890ABCDEF", 10)
  .catch(console.error);
```

---

## ステップ 4：他の人の NFT を購入する

売却オファーが出ている NFT を買いたい場合、**同じく `NFTokenCreateOffer` トランザクション** を使いますが、`Flags` が異なります。

```javascript
async function buyNFT(issuer, tokenID, priceInXRP) {
  await client.connect();

  const wallet = xrpl.Wallet.fromSeed(process.env.TESTNET_SEED);

  // 購入オファー作成
  const transactionJson = {
    TransactionType: "NFTokenCreateOffer",
    Account: wallet.address,
    TokenID: tokenID,
    Amount: xrpl.xrpToDrops(priceInXRP),
    Owner: issuer,  // 売り手のアカウント
    // Flags を指定しない = 買い手オファー
  };

  const tx = await client.submitAndWait(transactionJson, { wallet });

  console.log(`✅ 購入オファー作成成功！`);
  console.log(`TokenID: ${tokenID}`);
  console.log(`Offer Amount: ${priceInXRP} XRP`);

  // 売却オファーと自動マッチング される場合、ここで NFT 所有権が移行
  const accountNFTs = await client.request({
    command: "account_nfts",
    account: wallet.address,
  });

  const purchased = accountNFTs.result.account_nfts.find(
    (nft) => nft.token_id === tokenID
  );

  if (purchased) {
    console.log(`🎉 NFT 購入完了！あなたが所有者になりました。`);
  }

  await client.disconnect();
}

buyNFT("rN7n7otQDd6FczFgLdZqwcjwyvWtQaYeMi", "000800001234567890ABCDEF1234567890ABCDEF", 10)
  .catch(console.error);
```

---

## トラブルシューティング

### Q: 「Not enough XRP to create offer」エラーが出た

**A:** リザーブ不足です。XRPL では各アカウントが最低限度額の XRP を保持する必要があります（約 10 XRP）。Testnet で無料配布を受けてください。

### Q: トランザクション署名に失敗する

**A:** Secret Seed が正しいか確認してください。`.env` ファイルに正確にコピーされているか double-check。

### Q: 「Offer` と「Sell Offer」の違いがわからない

**A:** 
- **Offer**（Flags なし）= 買い手が「このお金で売ってくれませんか？」と提示
- **Sell Offer**（Flags: 1）= 売り手が「このお金で売ります」と宣言

マッチング条件が合えば自動的に取引成立。

---

## まとめ

XRPL NFT は複雑そうに見えますが、実は：

1. **ミント** = `NFTokenMint` トランザクション
2. **売却** = `NFTokenCreateOffer` + `Flags: 0x01`
3. **購入** = `NFTokenCreateOffer` +（Flags なし）

3 つの操作を理解すれば、マーケットプレイスから個人間取引まですべて実装できます。

ガス代がなく、確認が早い XRPL だからこそ、NFT を試して遊ぶなら Testnet で今すぐ始めてください。

> 次のステップ：
> - マーケットプレイス UI を Next.js で構築
> - NFT メタデータ（画像 + JSON）の管理方法
> - ロイヤリティ機能（TransferFee）の活用

---

## 参考リンク

- [XRPL NFT Documentation](https://xrpl.org/nftokenmint.html)
- [xrpl.js GitHub](https://github.com/XRPLF/xrpl.js)
- [Testnet Explorer](https://testnet.xrpl.org)
- [XRPL Concepts: NFTs](https://xrpl.org/non-fungible-tokens.html)
